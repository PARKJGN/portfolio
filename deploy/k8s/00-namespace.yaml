# portfolio 전용 네임스페이스. 이미 있으면 apply 해도 무해하다(멱등).
#
# 원래 001 의 Phase 6(T042)이 만드는 파일이다. 여기 먼저 두는 이유는, 이것이 없으면
# 60-api.yaml 도 15-config.yaml 도 apply 되지 않아 003 의 매니페스트만으로는 아무것도
# 확인할 수 없기 때문이다. 001 이 Phase 6 를 할 때 이 파일을 다시 만들지 말고 그대로 쓴다.
apiVersion: v1
kind: Namespace
metadata:
  name: portfolio
